info:
  name: get all user
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:2000/api/user/view-all
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
