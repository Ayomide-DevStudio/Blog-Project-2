info:
  name: edit blog
  type: http
  seq: 10

http:
  method: PUT
  url: http://localhost:2000/api/blog/edit-blog/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
