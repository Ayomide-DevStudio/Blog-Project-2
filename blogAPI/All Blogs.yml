info:
  name: All Blogs
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:2000/api/blog/getAllBlogs
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
